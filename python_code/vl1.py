import pandas as pd
import numpy as np
import matplotlib.pyplot as plot

# understand the problem
	# what is the data
	# what are the unknowns
	# what is is/are the condition(s) linking the data
# devise a plan
# carry out the plan
# look back


# Anzahl verkaufter Zeitungen pro Tag an einem Kiosk
# Ein Kioskbesitzer notiert 200 Tage lang die Anzahl der verkauften Exemplare einer Zeitung.
# Urliste sortiert nach Groesse

v = np.array(0) # vector is a sequence of elements, it can be thought as a list of values
v2 = np.array(1)
np.repeat(v, 21)
np.repeat(v2, 46)

# concatenate arays 
# create dataframes or matrices
# make calcluations
# plot results


